<div class="about ten columns offset-by-two">
	<form action="/blog/edit" method="POST" class="pure-form">
		  <input type="hidden" name="id" value="{{.ObjId | formatId}}">
	      <input type="text" name="title" value="{{.Title}}"/>
	      <textarea class="monospace" name="text">{{.Text}}</textarea> 
	      <input type="submit" value="submit" class="pure-button pure-button-primary"/>
	</form>
</div>